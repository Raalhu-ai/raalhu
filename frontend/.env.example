# Google Analytics tracking ID (optional — leave empty to disable)
PUBLIC_GA_ID=

# Backend URL (used by SvelteKit server routes, defaults to http://localhost:3000)
# Set in Cloudflare Pages environment for production
BACKEND_URL=http://localhost:3000
